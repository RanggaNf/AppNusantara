<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white"
    tools:context=".AboutActivity">


    <ImageView
        android:id="@+id/image_profile"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:background="@drawable/circle_background"
        android:elevation="4dp"
        android:scaleType="centerCrop"
        android:src="@drawable/foto_me" />

    <!-- Nama -->
    <TextView
        android:id="@+id/text_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_profile"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="MOHAMMAD RANGGA NUR FAIZIN"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Email -->
    <TextView
        android:id="@+id/text_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_name"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="email@example.com"
        android:textSize="14sp" />

    <!-- Media Sosial -->
    <TextView
        android:id="@+id/text_social_media"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_email"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="Sosial Media: @rangfaziii"
        android:textSize="14sp" />

    <!-- Tombol Hubungi Kontak lebih kecil -->
    <Button
        android:id="@+id/btn_contact"
        android:layout_width="116dp"
        android:layout_height="48dp"
        android:layout_below="@+id/text_social_media"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:background="@drawable/rounded_button"
        android:paddingStart="12dp"
        android:paddingEnd="6dp"
        android:text="Kontak"
        android:textColor="@android:color/white" />

</RelativeLayout>
